<%
  optional_classes = {
    'border-red-500': question.errors.any?
  }
 %>
<div class="grid grid-flow-col">
  <%= form.select :value, question.choices, { include_blank: "Choose an option..."}, class: "rounded-md bg-gray-50 border-gray-300 w-full text-gray-700 #{class_names(optional_classes)}" %>
</div>